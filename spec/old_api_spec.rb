require File.expand_path(File.join(File.dirname(__FILE__), "spec_helper.rb"))

describe TokyoCabinet do
  before(:each) do
    
  end
  
  it "should support old-style method calls when overriden"
end
